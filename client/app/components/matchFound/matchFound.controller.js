class MatchFoundController {
  constructor(moment) {
    //let dayWrapper = moment().format();
    this.name = 'matchFound';
  }
}

MatchFoundController.$inject = ['moment'];
export default MatchFoundController;
